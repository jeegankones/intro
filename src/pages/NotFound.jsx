import React from 'react';

const NotFound = () => (
  <div className="mt-2 mt-md-4">
    <h1 className="mb-2">Oops, we couldn't find that page!</h1>
    <div>It's possible the link you followed is broken or out of date.</div>
  </div>
);

export default NotFound;
